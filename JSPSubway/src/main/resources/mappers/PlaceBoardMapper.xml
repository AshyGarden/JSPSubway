<?xml version="1.0" encoding="UTF-8"?>
<!DOCTYPE mapper PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN"
    "http://mybatis.org/dtd/mybatis-3-mapper.dtd">
    
<mapper namespace="com.spring.JspSubway.placeboard.mapper.IPlaceBoardMapper">

	<insert id="regist">
		INSERT INTO placeboard
		(user_id, title, content, addr_zip_num, addr_basic, addr_detail, place_url) )
		VALUES
		(#{userId}, #{title}, #{content}, #{addrZipNum}, #{addrDetail}, #{placeUrl})
	</insert>


</mapper>